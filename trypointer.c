#include <stdio.h>
#include <stdlib.h>
/*int main() {
  
  int *array;
  int n = 3;
  int i;
  array = new int [n];
  if (array == NULL) {
        // 無法取得記憶體空間
        fprintf(stderr, "Error: unable to allocate required memory\n");
        return 1;
  }
  array[0] =1;
  array[1] =2;
  array[2] = 3;
  for (i = 0; i < n; i++) {
    printf("%d ", array[i]);
  }
  delete array;     // 動態矩陣用完後還給系統, 這是好的習慣
  n = 5;
  array = new int [n];
  array[0] =4;
  array[1] =5;
  array[2] =6;
  for (i = 0; i < n; i++) {
    printf("%d ", array[i]);
  }
  return 0;
}*/